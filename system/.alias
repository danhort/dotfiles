alias ..='cd ..'
alias ...='cd ../..'
alias ....='cd ../../..'
alias dotfiles="cd $DOTFILES_PATH"

# Docker
alias dc=docker-compose

alias ll='ls -la --color=always'

# include local aliases
if [ -f "$DOTFILES_PATH"/local/.alias ]; then
  . "$DOTFILES_PATH"/local/.alias
fi
